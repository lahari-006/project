# Install these inside your SEPARATE ragas-env virtual env:
#   python -m venv ragas-env
#   source ragas-env/bin/activate   (or ragas-env\Scripts\activate on Windows)
#   pip install -r eval/requirements-eval.txt

ragas>=0.2.6
datasets>=2.20
pandas>=2.0
langchain-anthropic>=0.2
langchain-huggingface>=0.1
sentence-transformers>=3.0
